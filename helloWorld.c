#include <stdio.h> 

// O include funciona lembrando uma importação no python, esta é uma 
// palavra chave que nos permite declarar quais arquivos queremos incluir ao nosso arquivo. 
// O arquivo stdio é o padrão e inclui algumas funções importantes da línguagem c como funções 
// de entrada e saída. 

// Criando a função príncipal, que no caso é de tipo inteiro, int, pois retorna um inteiro. 

// Hello world - Imprimindo uma string.
int main(){


    // Nós utilizamos o /n para que o texto seguinte a ele seja escrito na próxima linha. 

    printf("Estamos no mes de dezembro!\n");
    printf("\nO mes de natal!");

    // Este return serve para dizer ao compilador que o código terminou, 
    // serve para verificar que código foi terminado com sucesso. 

    return 0;
}